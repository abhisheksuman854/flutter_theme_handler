import 'package:flutter/material.dart';

class DefaultColors {
  // App primary colors
  static const Color primary = Color.fromRGBO(255, 255, 255, 1);
  static const Color primaryDark = Color.fromRGBO(10, 10, 10, 1);

  // App on primary colors
  static const Color onPrimary = Color.fromRGBO(0, 57, 166, 1);
  static const Color onPrimaryDark = Color.fromRGBO(120, 129, 251, 1);

  // App secondary colors
  static const Color secondary = Color.fromRGBO(247, 146, 38, 1);
  static const Color secondaryDark = Color.fromRGBO(128, 128, 128, 1);

  // App surface colors
  static const Color surface = Color.fromRGBO(246, 246, 246, 1);
  static const Color surfaceDark = Color.fromRGBO(186, 186, 186, 0.2);

  // App on surface colors
  static const Color onSurface = Color.fromRGBO(68, 68, 68, 1);
  static const Color onSurfaceDark = Color.fromRGBO(186, 186, 186, 1);

  // App inverse surface colors
  static const Color inverseSurface = Color.fromRGBO(246, 246, 246, 1);
  static const Color inverseSurfaceDark = Color.fromRGBO(186, 186, 186, 0.6);

  // App primary Container
  static const Color primaryContainer = Color.fromRGBO(246, 246, 246, 1);
  static const Color primaryContainerDark = Color.fromRGBO(186, 186, 186, 0.2);

  // App primary Container
  static const Color onPrimaryContainer = primaryContainerDark;
  static const Color onPrimaryContainerDark = primaryContainer;

  // App error color
  static const Color error = Color.fromRGBO(227, 23, 16, 1);
  static const Color black = Colors.black;
}
